# Demo-only provider fixtures

These files are synthetic transport fixtures for validating SkyPilot's provider guards and normalizers. They are not real player, profile, auction, or market data and must never be presented as live Hypixel data.

The production API routes do not import or fall back to these fixtures. Missing credentials and upstream failures return explicit classified errors.

