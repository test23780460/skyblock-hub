services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - path: .env
        required: false
    environment:
      SITE_URL: http://localhost:3000
    restart: unless-stopped
